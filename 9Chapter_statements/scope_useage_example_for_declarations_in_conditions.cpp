#include <string>
#include <list>
#include <iostream>
using namespace std; 

/*It is best to introduce the variable into the smallest scope possible.*/

if (double d = prim(true))
{
	left /= d;
	break
}
